[MarkerType]
typedef ( ImageBitmap or HTMLCanvasElement or OffscreenCanvas ) GPUImageCopyExternalImageSource;

interface HTMLCanvasElement {
};

interface ImageBitmap {
};

interface OffscreenCanvas {
};

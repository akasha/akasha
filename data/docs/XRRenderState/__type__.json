
{
    "kind": "Type",
    "name": "XRRenderState",
    "href": "https://developer.mozilla.org/en-US/docs/Web/API/XRRenderState",
    "description": "The XRRenderState interface of the WebXR Device API contains configurable values which affect how the imagery generated by an XRSession gets composited.",
    "content": "<div class=\"blockIndicator draft\"> \n <p><strong>Draft</strong><br> This page is not complete.</p> \n</div>\n<div class=\"blockIndicator secureContexts\">\n <p><strong>Secure context</strong><br>This feature is available only in <a href=\"https://developer.mozilla.org/en-US/docs/Web/Security/Secure_Contexts\">secure contexts</a> (HTTPS), in some or all <a href=\"#Browser_compatibility\">supporting browsers</a>.</p>\n</div>\n<p class=\"syntaxbox\">The <code><strong>XRRenderState</strong></code> interface of the WebXR Device APIcontains configurable values whichaffect how the imagery generated by an<code>XRSession</code>gets composited.These properties include the range of distances from the viewer within which content should be rendered, the vertical field of view (for inline presentations), and a reference to the <code>XRWebGLLayer</code> being used as the target for rendering the scene prior to it being presented on the XR device's display or displays.</p>\n<p>When you apply changes using the<code>XRSession</code>method <code>updateRenderState()</code>, the specified changes take effect after the current animation frame has completed, but before the next one begins.</p>",
    "properties": [
        "baseLayer",
        "depthFar",
        "depthNear",
        "inlineVerticalFieldOfView"
    ]
}

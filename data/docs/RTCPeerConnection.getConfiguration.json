
{
    "name": "RTCPeerConnection.getConfiguration",
    "href": "https://developer.mozilla.org/en-US/docs/Web/API/RTCPeerConnection/getConfiguration",
    "description": "<p><strong>This is an <a href=\"https://developer.mozilla.org/en-US/docs/MDN/Contribute/Guidelines/Conventions_definitions#Experimental\">experimental technology</a></strong><br>Check the <a href=\"#Browser_compatibility\">Browser compatibility table</a> carefully before using this in production.</p>\n<p>The <strong><code>RTCPeerConnection.getConfiguration()</code></strong> method returns an <code>RTCConfiguration</code> object which indicates the current configuration of the <code>RTCPeerConnection</code> on which the method is called.</p>\n<p>The returned configuration is thelast configuration applied via <code>setConfiguration()</code>, or if <code>setConfiguration()</code> hasn't been called, the configuration the <code>RTCPeerConnection</code> was constructed with.The configuration includes a list of the ICE servers used by the connection, information about transport policies, and identity information.</p>"
}

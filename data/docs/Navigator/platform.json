
{
    "kind": "Property",
    "name": "Navigator.platform",
    "href": "https://developer.mozilla.org/en-US/docs/Web/API/Navigator/platform",
    "description": "Returns a string representing the platform of the browser. The specification allows browsers to always return the empty string, so don't rely on this property to get a reliable answer."
}

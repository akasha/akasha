
{
    "kind": "Method",
    "name": "WritableStream.getWriter",
    "href": "https://developer.mozilla.org/en-US/docs/Web/API/WritableStream/getWriter",
    "description": "The getWriter() method of the WritableStream interface returns a new instance of WritableStreamDefaultWriter and locks the stream to that instance. While the stream is locked, no other writer can be acquired until this one is released."
}

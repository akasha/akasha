
{
    "name": "RTCIceTransport.getRemoteCandidates",
    "href": "https://developer.mozilla.org/en-US/docs/Web/API/RTCIceTransport/getRemoteCandidates",
    "description": "The RTCIceTransport method getRemoteCandidates() returns an array which contains one RTCIceCandidate for each of the candidates that have been received from the remote peer so far during the current ICE gathering session.",
    "content": "<p>The <strong><code>RTCIceTransport</code></strong> method <code><strong>getRemoteCandidates()</strong></code> returns an array which contains one <code>RTCIceCandidate</code> for each of the candidates that have been received from the remote peer so far during the current <a href=\"https://developer.mozilla.org/en-US/docs/Glossary/ICE\">ICE</a> gathering session.</p>\n<p>Each time your signaling code calls <code>RTCPeerConnection.addIceCandidate()</code> to add a received candidate to the ICE session, the ICE agent places it in the list returned by this function.</p>"
}
